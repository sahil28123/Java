import java.io.*;
import java.util.*;

public class reversestring {
   public static void main(String[] args) {
      String input = "I love chai";
      char[] rev = input.toCharArray();
      for (int i = rev.length-1;i >= 0; i--) 
       System.out.print(rev[i]);
   }
}
